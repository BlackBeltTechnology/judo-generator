{
	"resources": [
		[% 	for (resource in Resources!Resource.all) { %]
		{
			"key": "[%=resource.key%]",
			"id": "[%=resource.uuid%]",
			"value": "[%=resource.value%]"
		}
		[% if(resource <> Resources!Resource.all.last) { %]
			,
		[% } %]
		[% } %]
	]
}

