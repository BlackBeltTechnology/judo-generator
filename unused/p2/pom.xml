 <?xml version="1.0" encoding="UTF-8"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd" xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>hu.blackbelt.judo.generator</groupId>
        <artifactId>generator-parent</artifactId>
        <version>2.0.0-SNAPSHOT</version>
        <relativePath>../parent</relativePath>
    </parent>

  <artifactId>generator-p2</artifactId>
  <description>Judo Generator p2 features</description>

    <build>
        <plugins>
            <plugin>
                <groupId>org.reficio</groupId>
                <artifactId>p2-maven-plugin</artifactId>
                <version>1.3.0</version>
                <executions>
                    <execution>
                        <id>create-p2</id>
                        <goals>
                            <goal>site</goal>
                        </goals>
                        <phase>package</phase>
                        <configuration>
                            <featureDefinitions>
                                <feature>
                                    <id>hu.blackbelt.judo.generator.feature</id>
                                    <version>${project.version}</version>
                                    <label>Judo Generator features</label>
                                    <providerName>http://www.blackbelt.hu/judo</providerName>
                                    <description>${project.description}</description>
                                    <copyright>A copyright</copyright>
                                    <license>A licence</license>
                                    <artifacts>
                                        <artifact>
                                            <id>com.google.googlejavaformat:google-java-format:jar:1.4</id>
                                        </artifact>
                                    </artifacts>
                                </feature>
                            </featureDefinitions>
					   <categoryFileURL>${basedir}/category.xml</categoryFileURL>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
          <plugin>
                <groupId>org.eclipse.tycho</groupId>
                <artifactId>tycho-p2-repository-plugin</artifactId>
                <version>${tycho.version}</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>archive-repository</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- Attach zipped P2 repository to be installed and deployed
                in the Maven repository during the deploy phase. -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <version>3.0.0</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>attach-artifact</goal>
                        </goals>
                        <configuration>
                            <artifacts>
                                <artifact>
                                    <file>target/${project.artifactId}-${project.version}.zip</file>
                                    <type>zip</type>
                                </artifact>
                            </artifacts>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        
        </plugins>
    </build>
</project>

    
