[* Postgre/Oracle *]

CREATE TABLE [%=tableOperation.table.sqlName%] (
	ID INTEGER NOT NULL,
	[%for (field in tableOperation.table.fields) {%]
	[%=field.sqlName%] [%=field.type.name%][% if (field.mandatory) { out.print("NOT NULL"); }%],
	[%}%]
	CONSTRAINT ID_PK PRIMARY KEY (ID)
	);[% out.println("");%]