[* Hsql *]

CREATE TABLE [%=tableOperation.table.sqlName%] (
	ID INTEGER NOT NULL,
	[%for (field in tableOperation.table.fields) {%]
	[%=field.sqlName%] [%=field.type.toString()%] [% if (field.mandatory) { out.print("NOT NULL"); }%] [%if (hasMore) { out.print(","); }%]	
	[%}%]
	PRIMARY KEY (ID)
	);[% out.println("");%]